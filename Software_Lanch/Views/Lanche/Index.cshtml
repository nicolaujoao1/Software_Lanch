@model LanchListViewModel

<div>
    <h1>Todos os lanches</h1>
</div>
<div class="container">
    <div class="row">

        @foreach (var lanch in Model.Lanches)
        {
            <partial name="_LanchesResumo" for="@lanch"></partial>
        }

    </div>
</div>